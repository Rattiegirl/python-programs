# 🚢 Battleship Game

Классическая игра "Морской бой" с несколькими режимами игры и веб-интерфейсом.

## 📋 Описание проекта

Этот проект представляет собой реализацию игры "Морской бой" с различными режимами:
- **Консольная игра** - игра против компьютера в терминале
- **Веб-версия** - игра через браузер с графическим интерфейсом
- **Бот против бота** - автоматическая игра между двумя ИИ
- **Дополнительные утилиты** - магазин и список задач

## 🛠 Технологический стек

### Основные технологии:
- **Python 3.x** - основной язык программирования
- **FastAPI** - веб-фреймворк для API и веб-интерфейса
- **Jinja2** - шаблонизатор для HTML
- **Pydantic** - валидация данных
- **HTML/CSS/JavaScript** - фронтенд для веб-версии

### Структура проекта:
```
battleship/
├── battleship/           # Основной модуль игры
│   ├── __init__.py
│   ├── config.py         # Конфигурация игры
│   └── lib.py           # Основная логика игры
├── templates/            # HTML шаблоны
│   └── index.html       # Веб-интерфейс
├── pictures/            # Изображения для веб-версии
│   ├── bubbles.png
│   └── sinking_ship.png
├── battleship.py        # Консольная версия игры
├── server.py           # Веб-сервер
├── double_bot_battleship.py  # Игра бот против бота
├── shop.py             # Утилита магазина
├── todo.py             # Утилита списка задач
└── readme.md           # Документация
```

## 📦 Установка зависимостей

### Требования к системе:
- Python 3.7 или выше
- pip (менеджер пакетов Python)

### Установка зависимостей:

```bash
pip install fastapi uvicorn jinja2 pydantic
```

Или создайте файл `requirements.txt`:
```
fastapi>=0.68.0
uvicorn>=0.15.0
jinja2>=3.0.0
pydantic>=1.8.0
```

И установите:
```bash
pip install -r requirements.txt
```

## 🎮 Режимы игры

### 1. Консольная игра (`battleship.py`)
Классическая версия игры в терминале.

**Запуск:**
```bash
python battleship.py
```

**Управление:**
- Введите координаты в формате `a1-j10` (например: `b5`, `d8`)
- `gg` - завершить игру
- `break` - прервать игру

### 2. Веб-версия (`server.py`)
Современный веб-интерфейс с графическим отображением поля.

**Запуск:**
```bash
uvicorn server:app --reload
```

**Доступ:**
- Откройте браузер и перейдите по адресу: `http://localhost:8000`
- Играйте, кликая по клеткам поля

### 3. Бот против бота (`double_bot_battleship.py`)
Автоматическая игра между двумя ИИ-противниками.

**Запуск:**
```bash
python double_bot_battleship.py
```

## ⚙️ Конфигурация игры

Настройки игры находятся в файле `battleship/config.py`:

```python
SHIPS = [4, 3, 3, 2, 2, 2, 1, 1, 1, 1]  # Корабли: 1x4, 2x3, 3x2, 4x1
ROWS = 10  # Размер поля по вертикали
COLS = 10  # Размер поля по горизонтали
```

## 🎯 Правила игры

1. **Расстановка кораблей:**
   - 1 корабль на 4 клетки
   - 2 корабля на 3 клетки
   - 3 корабля на 2 клетки
   - 4 корабля на 1 клетку

2. **Ход игры:**
   - Игроки по очереди стреляют по координатам
   - При попадании ход продолжается
   - При промахе ход переходит к противнику

3. **Обозначения на поле:**
   - `.` - пустая клетка
   - `#` - корабль
   - `X` - подбитый корабль
   - `~` - промах
   - `-` - область вокруг потопленного корабля

## 🔧 Дополнительные утилиты

### Магазин (`shop.py`)
Простая система управления товарами с функциями:
- Добавление товаров
- Просмотр ассортимента
- Удаление товаров
- Покупка товаров

**Запуск:**
```bash
python shop.py
```

### Список задач (`todo.py`)
Менеджер задач с функциями:
- Добавление задач
- Удаление выполненных задач
- Просмотр списка задач

**Запуск:**
```bash
python todo.py
```

## 🚀 Быстрый старт

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd battleship
```

2. **Установите зависимости:**
```bash
pip install fastapi uvicorn jinja2 pydantic
```

3. **Запустите веб-версию:**
```bash
uvicorn server:app
```

4. **Откройте браузер:**
```
http://localhost:8000
```

## 📁 Структура файлов

### Основные модули:
- `battleship/lib.py` - основная логика игры (создание поля, расстановка кораблей, проверка попаданий)
- `battleship/config.py` - настройки игры
- `server.py` - FastAPI веб-сервер
- `templates/index.html` - веб-интерфейс

### Вспомогательные файлы:
- `pictures/` - изображения для веб-интерфейса
- `seas.json` - сохранение состояния игры (создается автоматически)

## 🎨 Особенности реализации

- **Сохранение состояния** - игра автоматически сохраняется и может быть продолжена
- **Валидация ввода** - проверка корректности координат
- **Адаптивный дизайн** - веб-интерфейс работает на разных устройствах
- **Модульная архитектура** - легко расширяемая структура кода

## 🤝 Вклад в проект

Проект открыт для улучшений! Вы можете:
- Добавить новые режимы игры
- Улучшить ИИ противника
- Добавить мультиплеер
- Улучшить веб-интерфейс

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

